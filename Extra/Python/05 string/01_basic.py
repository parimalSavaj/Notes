# we can string create using three way.
a = 'parimal' # using single quotes.
b = "parimal" # also using multi quotes.
c = '''pari
mal''' # using single quotes with three time.
d = """pari
mal""" # using multi quotes with three time.


# string just like array. means we can loop on string and access using position index.
name = "parimal";
print(name[0]);

# inside string have negative indexing as well.
print(name[-1]);

# find string length
print(len(name))